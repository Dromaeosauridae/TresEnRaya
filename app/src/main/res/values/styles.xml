<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ficha</item>
        <item name="colorPrimaryDark">@color/fondo</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/aclonica</item>
        <item name="android:windowBackground">@drawable/fondo_juego</item>
    </style>

    <style name="Casilla" >
        <item name="android:freezesText">true</item>
        <item name="android:textColor">@color/ficha</item>
        <item name="android:textSize">80sp</item>
        <item name="android:background">@color/tablero</item>
    </style>

    <style name="Marcador">
        <item name="android:freezesText">true</item>
        <item name="android:textColor">@color/tablero</item>
        <item name="android:textSize">40sp</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="Boton">
        <item name="android:textColor">@color/ficha</item>
        <item name="android:textSize">25sp</item>
        <item name="android:padding">10dp</item>
        <item name="android:background">@color/tablero</item>
    </style>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
</resources>
